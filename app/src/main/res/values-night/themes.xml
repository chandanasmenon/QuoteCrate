<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.QuoteCrate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="ToolbarTitleStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/acme</item>
        <item name="android:textColor">@color/black</item> <!-- Optional -->
    </style>

</resources>